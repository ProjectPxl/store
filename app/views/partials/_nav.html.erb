<section class="nav">

  <div class="grid-small">  
    <ul>
      <% @collections.each_with_index do |col, i| %>
        <li class="nav-item <%= @collections.size-1 == i ? 'omega' : '' %> ">
          <%= link_to col.display_name, collection_path(col.name) %>
        </li>
      <% end %>
    </ul>
  </div> <!-- grid -->

</section>