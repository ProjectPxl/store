<section class="">
	<div class="grid">
		<ul>
			<% @products.each do |product| %>
				<li>
					<%= link_to image_tag("product_images/#{}"), edit_admin_product_path(product) %>
				</li>
			<% end %>
		</ul>

		<%= link_to "New product", new_admin_product_path %>
	</div>
</section>
